// SIGARAM64 — App
// Route definitions have been moved to src/config/routes.tsx
// This file is kept for backward compatibility but is no longer used as the routing entry point.
// The app now uses createBrowserRouter + RouterProvider (Data Router pattern).
// See src/config/routes.tsx for all route definitions.
