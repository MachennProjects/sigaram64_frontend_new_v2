// SIGARAM64 — Firebase (DISABLED)
// Firebase has been replaced by the new Express/MongoDB REST backend.
// This file is kept for reference only. Do NOT import from it.
// All auth/data is now handled via src/api/ using JWT tokens.

export const auth    = null;
export const db      = null;
export const storage = null;
export const firebaseConfig = {};
